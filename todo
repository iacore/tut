[Exit compose view?] confirm dialog is fullscreen. it should be a small popup window.

Loading new posts in timeline freezes the UI.
Race conditions. Opening multiple threads in one panel might race condition and mess up history.
kitty image protocol

switch keymap to kakoune


## stream refactor

stream cached inside ac.Streams
each stream can have multiple receivers
receivers close themselves when dropped

## feed refactor (feed/feed.go, ui/feed.go)

desktop feed. based on streams.

todo: turn the API from pull based to push based

